# Integrations Package

Phase 0 scaffold for consolidating external adapters.

- `zabbix/`: target location for the resilient client, guards, and services
  currently under `zabbix_api/`.
- Additional domains (CRM, OSS, etc.) should live in siblings of `zabbix/`.

Refer to `doc/developer/REFATORAR.md` for the migration roadmap.
